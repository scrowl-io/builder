{{#with . as | data |}}
import React from 'react';
import * as styles from './styles.module.scss';
import { Slide, Manifest } from "./slide"
import { ComponentExample } from "./components"

export interface {{data.pascal}}Props {
  manifest: Manifest
}

export const {{data.pascal}} = ({ manifest }: {{data.pascal}}Props) => {
  return (
    <Slide className={{reactClassName data.camel}}>
      <ComponentExample data={manifest.elements.componentExample} />
    </Slide>
  )
};

export default {
  {{data.pascal}},
};
{{/with}}
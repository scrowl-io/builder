{{#with . as | data |}}
import React from 'react';
import * as styles from './styles.module.scss';
import { ManifestElements } from "./slide"
import { ComponentExample } from "./components"

export interface {{data.pascal}}Props {
  manifest: ManifestElements
}

export const {{data.pascal}} = ({ manifest }: {{data.pascal}}Props) => {
  return (
    <div className={styles.imageGrid}>
      <ComponentExample options={manifest.componentExample} />
    </div>
  )
};

export default {
  {{data.pascal}},
};
{{/with}}